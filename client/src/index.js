/* eslint-disable prop-types */
/* eslint-disable no-unused-vars */
/* eslint-disable no-undef */
// We should remove these eslint checks when we load our real component

import ReactDOM from 'react-dom';
import React from 'react';
// import your main App Component here
// import YourComponent from 'components/YourComponent'

const wrapper = document.getElementById('app');
// Uncomment the following lines to mount the main component
// wrapper ? ReactDOM.render(<YourComponent />, wrapper) : false
